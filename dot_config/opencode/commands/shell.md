---
description: Run the following text as a literal shell command, exactly as written
---

Treat `$ARGUMENTS` as a literal shell command and execute it immediately with the bash tool.

- Do not rewrite, explain, or "improve" the command before running it.
- Do not inspect the repository first unless the command itself requires repository context.
- Run the command first, then briefly report the exit status and any important stdout or stderr.
- If no command was given, ask the user which command to run.
